/// @description 
// Rotation
rotX = -30;
rotZ = 0;

// Position
z = depth;

// Mouse
sensX = 0.2;
sensY = 0.2

mouseX = device_mouse_x(0);
mouseY = device_mouse_y(0);

// Collision shape
col = ColShape(x, y, 32);